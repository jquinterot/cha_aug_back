apiVersion: v1
kind: Secret
metadata:
  name: chat-aug-secrets
type: Opaque
data:
  # These values should be base64 encoded
  # Example: echo -n "your-mongodb-uri" | base64
  mongo-uri: ""  # Add your base64 encoded MongoDB URI
  openai-api-key: ""  # Add your base64 encoded OpenAI API key
